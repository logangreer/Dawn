{{!< default}}

<div class="content-area">
    <main class="site-main">
        <div class="container medium">
            <h2 class="featured-title">All Posts</h2>
        </div>
        <div class="post-feed container medium">
            {{#foreach posts visibility="all"}}
                {{> "loop"}}
            {{/foreach}}
        </div>
        {{pagination}}
    </main>
</div>